{
  "name": "lede-tech",
  "version": "1.0.0",
  "private": true,
  "hexo": {
    "version": "3.2.0"
  },
  "dependencies": {
    "forever": "0.15.3",
    "hexo": "3.2.0",
    "hexo-asset-image": "0.0.2",
    "hexo-filter-cleanup": "1.0.6",
    "hexo-generator-archive": "0.1.4",
    "hexo-generator-category": "0.1.3",
    "hexo-generator-index": "0.2.0",
    "hexo-generator-tag": "0.2.0",
    "hexo-renderer-ejs": "0.2.0",
    "hexo-renderer-marked": "0.2.10",
    "hexo-renderer-stylus": "0.3.1",
    "hexo-server": "0.2.0"
  },
  "scripts": {
    "update": "rm -r -f source/_posts && git clone -q https://git.ms.netease.com/lede/blog.git source/_posts",
    "clean": "rm -f source/_posts/README.md",
    "generate": "npm run update && npm run clean && hexo g",
    "g": "npm run update && npm run clean && hexo g",
    "release": "cnpm install",
    "start": "cnpm cache clean && cnpm install && forever -p ./ -l ./logs/access.log -a start app.js",
    "hello": "node app.js && service nginx start ",
    "stop": "forever stop app.js"
  }
}
